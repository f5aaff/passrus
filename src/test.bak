mod test {
    use ratatui::prelude::*;
    use ratatui::widgets::{Input, Text};

    fn input_page() -> Box<dyn Widget> {
        let input = Input::new("Enter text:").on_submit(|text| {
            // Handle the submitted text (e.g., switch to the table page)
            // ...
        });

        let content = Column::new()
            .push(Text::new("Welcome to the Input Page!"))
            .push(input);

        Box::new(content)
    }

    use ratatui::widgets::{Table, Text};
    fn table_page() -> Box<dyn Widget> {
        // Replace with your actual data and search functionality
        let data = vec![
            vec!["Name", "Age"],
            vec!["Alice", "25"],
            vec!["Bob", "30"],
            // ...
        ];

        let table = Table::new(data)
            .header_style(Style::default().fg(Color::Yellow))
            .widths(&[Constraint::Length(20), Constraint::Length(10)]);

        let content = Column::new()
            .push(Text::new("Welcome to the Table Page!"))
            .push(table);

        Box::new(content)
    }
}
